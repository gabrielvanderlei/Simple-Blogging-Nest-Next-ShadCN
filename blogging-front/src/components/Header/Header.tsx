import {
  Card,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"

function Header() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Simple Blogging UI</CardTitle>
      </CardHeader>
    </Card>
  );
}

export default Header;
